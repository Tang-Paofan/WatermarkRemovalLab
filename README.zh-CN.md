# Watermark Removal Lab

[English](README.md) | [简体中文](README.zh-CN.md)

基于 OpenCV、SAM/SAM 2、LaMa、ONNX Runtime 和 FFmpeg 的图片与视频可见水印定位、分割、补全、评测和部署实验平台。

> [!IMPORTANT]
> 本项目仅用于您拥有、创建或已获授权编辑的图片和视频。项目不用于绕过版权保护、来源证明系统、平台声明或付费预览水印。

## 媒体支持

| 媒体 | 状态 | 规划流水线 |
|---|---|---|
| 图片 | 开发中 | 检测 / 提示 → SAM → LaMa |
| 视频：固定水印 | 已规划 | 静态掩膜 → 逐帧补全 |
| 视频：移动水印 | 已规划 | SAM 2 跟踪 → 时序补全 |

## 项目目标

Watermark Removal Lab 是一个工程实践和学习项目，用于探索：

- 可见水印检测与交互式提示；
- 掩膜分割、细化与传播；
- 图片补全和具有时序一致性的视频补全；
- OpenCV 与模型方法的质量基线；
- ONNX Runtime 模型部署与性能调优；
- FFmpeg 视频解码、编码与音频透传；
- 对质量、延迟和资源占用进行可复现评测。

项目采用可替换的流水线：

```text
检测 / 提示
    → 分割 / 定位
    → 细化掩膜
    → 补全
    → 评测
    → 输出
```

定位与补全保持解耦，使检测器、分割器、掩膜细化器、补全器和质量评估器可以独立演进。

## 开发路线

1. 使用手工区域和 Telea/Navier-Stokes 补全实现 OpenCV 图片基线。
2. 使用局部裁剪和仅掩膜区域回贴实现 LaMa 图片补全。
3. 实现带图像嵌入缓存的 SAM/SAM 2 交互式图片分割。
4. 基于模板、视觉特征和检测器实现通用图片水印定位。
5. 构建多模型流水线以及可复现的质量和性能评测。
6. 使用 FastAPI 和 Web 界面进行部署，支持 CPU/CUDA 与 ONNX Runtime。
7. 使用 FFmpeg 和音频透传实现固定区域视频处理基线。
8. 实现 SAM 2 掩膜传播与具有时序一致性的视频补全。

## 设计原则

- 通用 MVP 优先支持用户点击、框选或提供掩膜。
- SAM/SAM 2 用于提示分割、掩膜细化和跟踪，不默认作为理解水印语义的检测器。
- LaMa 处理带有留白的局部裁剪，回贴时仅替换细化掩膜内的像素。
- 保留 OpenCV Telea 和 Navier-Stokes 作为透明、无模型的基线。
- 在实现并评测时序一致性之前，如实将逐帧视频处理描述为工程基线。
- 模型资源缓存在 Git 仓库之外，并分别审计代码、权重和数据集许可证。

## 负责任使用

本项目仅用于您拥有或有权修改的内容。示例和测试素材应由项目成员自行创建、合成，或具有明确许可。项目不提供用于移除图库付费预览水印的平台专用检测器，也不提供以移除法定 AI 标签、来源元数据或来源归属为卖点的工具。

本说明仅描述项目的预期用途，不构成法律意见。

## 开发状态

仓库正在初始化。首个实现里程碑将提供 OpenCV 图片命令行工具、二值掩膜工具和最小自动化测试。

## 许可证

本仓库源代码采用 [Apache License 2.0](LICENSE) 许可。第三方模型、权重、数据集和随附资源可能采用不同许可证，并将在分发前单独记录。
